#include <SFML/Graphics.hpp>
#include <time.h>
using namespace sf;

int main(){
    //CREANDO VENTANA DEL JUEGO

    RenderWindow window(VideoMode(320, 480), "Tetris");

    while (window.isOpen()){
        
    }
}